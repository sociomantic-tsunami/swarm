## State-machine mixin deprecated

`swarm.neo.util.StateMachine`

Any requests still using this mixin should be converted to use one of the
request core mixins in `swarm.neo.client.mixins`, instead.

